ќбратна€ польска€ запись
(3+5)*10

3
5
+
10
*

в файле команды:

push -d
add
mul
div
sub
out
end
in

in - берет число из консоли

заменим команды на числа посредством компил€тора (asm - программа транслировани€)
числа - байт кодом

ассемблер и дизассемблер

загрузка и выгрузка в двоичном виде
		    в текстовом виде - листинг файл

16-ти рична€ запись
0000	01 00 00 00	03 00 00 00	push 3
0008	01 00 00 00	05 00 00 00	push 5
0010	02 00 00 00			add


и забьем все в буфер - ip(instruction pointer)

переменные(регистры процессора)
rax
rbx
rcx
rdx

push 5			->	1	5
push ax 		->	10	1

pop ax - из стека достаетс€ число и закидываетс€ в ax
push [50] - запушить 50ую €чейку оперативы
push [ax] - запушить ax-ую €чейку

jmp	-n
ja	-n	if s.pop > s.pop
jb	-n	if s.pop < s.pop
je	-n	if s.pop == s.pop
номер €чейки

sscanf()
midnight_comander 